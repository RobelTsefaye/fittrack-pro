# Offline-First Roadmap

**Branch:** `feature/offline-phase5-background-sync` (Phases 1-4 on `main`)
**Status:** Phase 5 code complete, on-device test still needed before merge.
**Last updated:** 2026-07-11

## Why this is a real rearchitecture, not a patch

Two structural facts about the current app make "just works offline" impossible without changing them:

1. **The native shell loads the entire web app from a live URL every launch.**
   `capacitor.config.ts` points `server.url` at `https://fittrack-pro-ashen.vercel.app` instead of bundling static files, explicitly because the app has server-only routes (API endpoints, Prisma, NextAuth) that can't be statically exported as-is. With zero network, the WKWebView has nothing to load — not even a blank shell, no error screen, nothing.
2. **There is no local data store.** Every screen fetches directly from a Postgres-backed API route. There's no cache, no offline write queue, nothing to fall back to.
3. **Auth is server-side session cookies**, checked in Server Components. 17 files under `src/app/(app)/` call `await auth()` server-side and `redirect("/login")` (a server-only API) if it's missing — this is essentially every screen in the app, not an edge case.

Fixing this properly means: bundle the UI locally (so the app always opens), move auth to a token model the native shell owns (so a static/client-rendered UI can still know who's logged in), then layer local caching and an offline write queue on top. Each phase below is independently shippable and testable before starting the next.

## Phases

### Phase 1 — Native token-based auth (no offline behavior change yet)
**Goal:** the native app authenticates via a stored Bearer token instead of (or alongside) the session cookie, so later phases have something to authenticate with once the cookie-based server session is gone.

- [x] Add local secure token storage on-device — `NativeAuthTokenPlugin.swift` (Keychain), deliberately separate from the existing `SyncTokenPlugin`/"Für Hintergrund-Sync verwenden" token (that one's an opt-in native-background convenience; this one is becoming the app's actual login credential — see the plugin's doc comment)
- [x] Native login flow: `POST /api/auth/native-login` (new route) → mints an `ApiToken` row via the existing `hashApiTokenSecret`/`resolveUserIdForDataApi` path (same one `WatchAPIProxy`/the PiP SSE stream already use) → stored via the plugin above. Wired additively into `login-form.tsx`: fires only when `Capacitor.isNativePlatform()`, alongside the unchanged cookie `signIn()` call, best-effort (a failure here never blocks login)
- [x] `authenticatedFetch()` helper (`src/lib/native/native-auth-token.ts`) attaches the stored token as `Authorization: Bearer` when present, falls back to plain `fetch` (cookie auth) otherwise — not wired into any page yet, that's Phase 2's job; exists now so it's independently testable
- [x] **Tested:** `/api/auth/native-login` end-to-end against the dev DB (wrong password → 401, correct password → token, token successfully authenticates a Bearer-protected route, temp user cleaned up). Existing cookie-based browser login re-verified unaffected (redirects to dashboard, no console errors)
- [x] **On-device tested** (Xcode console + Safari Web Inspector, real device): `save`/`load`/`clear` round trip confirmed correct — save → `{success:true}`, load → `{token:"hallo"}`, clear → `{success:true}`, load after clear → `{token:null}`. Full App-target build (App + Watch + Complication + Widget) also verified to compile clean via `xcodebuild`.

### Phase 2 — Bundle the UI locally; app always opens
**Goal:** the app opens instantly with zero network — proves the single biggest complaint is fixed, before any caching/sync work exists.

- [x] Convert `(app)` layout + all 18 server-auth-gated pages (17 originally audited + `plans/[planId]`, found necessary mid-phase) from server-side `auth()`/`redirect()` to client-side (`useAuthGate`/`RequireAuth`, token from Phase 1)
- [x] Convert 13 API routes from cookie-only `auth()` to `resolveUserIdForDataApi()` (dual cookie + Bearer), add 3 new bundling routes (`/api/records`, `/api/dashboard/client-payload`, `/api/dashboard/muscle-heatmap`) so client components don't need N separate calls
- [x] Global `fetch` patch (`NativeAuthFetchPatch`) attaches the Bearer token and rewrites relative URLs to the production origin — retrofits ~25 pre-existing components' plain `fetch()` calls without touching each one
- [x] Switch Next.js to static export (`output: "export"`) for native builds only (`NATIVE_BUILD=1`, see `next.config.ts`/`scripts/build-native.mjs`); API routes stay server-hosted on Vercel, moved out of the tree for the duration of that build only
- [x] Point Capacitor `webDir` at the exported output (`out/`); dropped `server.url` entirely — new `npm run sync:native` runs the export + `cap sync ios`
- [x] **Fixed 4 dynamic page routes** (`workouts/[id]`, `exercises/[id]`, `plans/[planId]`, `health/[metric]`) needing `generateStaticParams()` — client components can't export it themselves (needs a non-`"use client"` file), so each got a thin server wrapper `page.tsx` (just `generateStaticParams`) delegating to a `page-client.tsx` with the real logic. Placeholder param for the 3 truly dynamic ones (real id read via `useParams()` client-side after the SPA router navigates); `health/[metric]` lists all real slugs since they're known at build time.
- [x] **Fixed root layout** (`src/app/layout.tsx`): `resolveAppLocale()` calls `cookies()`/`auth()`/Prisma — none exist at static-export build time. Native builds now use a hardcoded `"en"` locale; real Vercel build unaffected. Per-user locale on native is a Phase 3+ concern.
- [x] **Verified:** `npm run build:native` produces a clean static export (38 pages, 0 errors); `npx next build` (normal Vercel path) re-verified unaffected afterward; `npx cap sync ios` copies it into `ios/App/App/public` correctly; full Xcode `App` target (App + Watch App + RestTimerWidget + Capacitor plugins) compiles clean against the synced bundle. (One unrelated pre-existing failure found in `FitTrackComplicationExtension` — a `#Preview` macro needing iOS 17 — confirmed via `git diff`/`git log` to be untouched by this phase; not fixed here, out of scope.)
- [x] **Tested on-device (iPad):** online — login, tab navigation, and all screens (dashboard, workouts, health + sub-screens, records, plans, exercises) load real data. Offline (airplane mode, force-quit, relaunch) — app opens to the full shell/dashboard frame, no white screen; data is absent (expected — Phase 3). Acceptance criterion met.

### Phase 3 — Local read cache (incremental, screen by screen)
**Goal:** each screen shows last-known data immediately, refreshes in the background when online, and doesn't go blank when offline.

- [x] **Discovered existing offline infrastructure predating this roadmap**: `src/lib/offline/db.ts` already had a working Dexie (IndexedDB) database with read caches + write queues for Workouts and Body Weight, plus a global `OfflineSyncProvider` that flushes queued writes on reconnect. This was from an earlier PWA-era effort, not reflected in this doc until now. Used Dexie (not `@capacitor-community/sqlite`) for everything below to stay consistent with what's already there — no reason to run two different local-storage mechanisms side by side.
- [x] **Fixed Workouts list**: `WorkoutHistoryList` already had a complete cache-then-network implementation (`workoutListCache` table) that was never actually invoked — `workouts/page.tsx` did its own separate network-only fetch instead and passed the result down as a prop, so offline this just hung on the page-level skeleton forever. Fixed by having the list own its entire fetch lifecycle (mount effect calls its own `fetchWorkouts`), matching the pattern `body-weight-tracker.tsx` already used correctly.
- [x] **Exercises list**: the exercise catalog cache (`saveExerciseCatalog`/`loadExerciseCatalog`, in `workout-offline-store.ts`) already existed and was being written to, but nothing ever read it back — a failed fetch just showed an empty list. Added the read-side fallback (`loadFromCatalogCache`, with client-side re-filtering by search/muscleGroup/equipment to match what the server would have returned).
- [x] **Dashboard, Achievements, Muscle Heatmap, Plans, Health** (dashboard + sleep/recovery/nutrition sub-screens): none of these had any offline cache at all. Added 5 new Dexie tables (`dashboardCache`, `achievementsCache`, `muscleHeatmapCache`, `plansCache`, `healthCache` — the last keyed per sub-screen) via a new `src/lib/offline/screen-caches.ts` (one small file instead of five near-identical ones, since these are pure read caches with no queue/conflict logic, unlike workouts/body-weight). Each screen now: checks `navigator.onLine` first (skips a doomed network round trip when already known offline), saves to cache on a successful fetch, and falls back to cache on either an offline start or a network failure.
- [x] **Verified in the browser**: visited every updated screen once online, then confirmed via direct IndexedDB inspection that all 7 new/fixed cache tables actually populated (previously 3 of them silently didn't — see below). Then overrode `navigator.onLine = false` and used in-app SPA link clicks (not a full browser reload, which would reset the override) to re-visit Workouts (empty-cache case) and Dashboard (data-bearing case) — both rendered from cache with zero new network requests fired, confirmed via the network log.
- [x] **Root-caused a debugging dead-end along the way**: initial testing showed `dashboardCache`/`achievementsCache`/`muscleHeatmapCache` staying empty even after adding temporary debug logging that never appeared in the console — traced to the app's pre-existing PWA service worker serving a **stale cached copy of the JS bundle** from an earlier test session (`fittrack-v13-pwa` cache), invisible to normal dev-server restarts because Next dev's chunk filenames aren't content-hashed (so the SW's cache-first match still "hit" on the old content under the same filename). Confirmed via a cache-busting fetch showing the current source WAS being served correctly once the SW's cache was actually bypassed. Not a Phase 3 bug — a pre-existing SW/dev-workflow gotcha, worth remembering for any future "my code changes don't seem to take effect in the browser" confusion.
- [x] `npx tsc --noEmit`, `npm run build` (normal Vercel path), and `npm run build:native` (static export) all clean.
- [x] **On-device test (iPad) passed**: airplane mode, force-quit, relaunch → app opens straight to the dashboard with last-known data (no blank screens, no login dead-end). Two auth bugs surfaced and were fixed during this test before it passed: (1) native login showed "Invalid email or password" when actually offline (a thrown fetch was indistinguishable from a rejected credential — now checks `navigator.onLine`); (2) an already-logged-in user was stranded on the landing/login screen offline because `RedirectIfAuthenticated` (the client-side equivalent of `middleware.ts`'s authed-user redirect, written in Phase 2) was never actually wired into any page — now wired into `/`, `/login`, `/register`. Minor known gap, accepted for now: the dashboard greeting falls back to a generic "Hi athlete" offline (the user's name isn't in the read cache yet).

### Phase 4 — Offline write queue
**Goal:** logging a set, starting/finishing a workout, etc. work offline and sync once back online.

- [x] **Discovered this is already substantially built**, predating this roadmap (same PWA-era effort noted in Phase 3): `src/lib/offline/db.ts`'s `queue`/`bodyWeightQueue` tables, `queue-types.ts` (covers post_workout/patch_workout/post_exercise/post_set/patch_set/delete_set/delete_we/complete_workout), `workout-offline-store.ts` (enqueue + snapshot read/write), `flush-workout-queue.ts` + `flush-body-weight-queue.ts` (replay against the API, remapping client-generated UUIDs to real server IDs as each op lands), `flush-all-queues.ts` (orchestrates both), and `OfflineSyncProvider` (mounted app-wide in `providers.tsx`, auto-flushes on the `online` event).
- [x] **Confirmed actually wired into the UI**, not dormant (unlike `RedirectIfAuthenticated` in Phase 3 — checked specifically for that failure mode again): `workout-detail.tsx`'s `useLocalWrites` flag (`!netOnline || offlineOriginSession || pendingQueue`) gates every mutation (rename, add exercise, add/edit/delete set, remove exercise, complete workout) between a direct API call and `enqueueWorkoutOp`+local state update; `set-row.tsx` takes an `offlineHandlers` prop for the same. `workouts/new/page.tsx`'s normal "Start" button already falls back to `startOffline()` automatically — both proactively (`!navigator.onLine` check) and reactively (network error mid-request) — no separate manual mode required for the common case.
- [x] **Verified the queue's own fetch calls should work under the native Bearer-token architecture**: they use relative `/api/...` paths with `credentials: "include"` (the pre-Phase-2 pattern), which `NativeAuthFetchPatch` (mounted globally, patches `window.fetch` itself) intercepts and reroutes through `CapacitorHttp.request()` with the Bearer token attached, regardless of which file the call originates from. Not yet confirmed on an actual device — this reasoning could still be wrong in a way only on-device testing would surface, same as every previous phase.
- [x] Basic conflict handling: replay-in-order is what the existing flush logic already does; adequate for a single-device-at-a-time user, per this doc's own explicitly-stated scope (multi-device simultaneous edits are out of scope).
- [x] **On-device test passed in full** (iPhone): airplane mode, start a blank workout via `/workouts/new`, log sets, add an exercise, delete a set, then both **cancel** and **finish** a workout — all confirmed working. Several blocking bugs surfaced and fixed along the way (see Log). **Phase 4 is done.**
- [ ] **Deferred until after this phase wraps**: starting a workout *from a plan session* (Dashboard "Next Workout" card, `next-workout-card.tsx`, and the plan detail view's start button, `plan-detail-view.tsx`) has no offline fallback — it's a single `fetch` with no queue/local-build path, so offline it just silently no-ops. Only the manual blank-workout start (`/workouts/new`) has the offline fallback. Needs the plan session template read from the existing `plansCache` (Phase 3) and the workout structure built client-side the way the server's `/api/plan-sessions/[id]/start` route does it.

### Phase 5 — Background sync polish + Watch interaction
**Goal:** tie the queue flush into the existing `BackgroundSyncManager` infra, and make sure the Watch's own native fallback (`WatchAPIProxy`) degrades sensibly if the phone itself is offline.

- [x] **Investigated "auto-flush via BackgroundSyncManager" and found it's not achievable as originally worded**: `BackgroundSyncManager` runs as a native `BGAppRefreshTask` with no WKWebView — it has no access to the IndexedDB write queue at all, which only exists inside the JS/WebView layer. It's HealthKit-only today and unrelated to the write queue. A true app-closed background flush of the JS queue isn't possible without running JS, which iOS doesn't allow in this context. Scoped down to the realistic version instead: the flush already runs on mount and on the `online` event (`OfflineSyncProvider`); added a `CapacitorApp` `resume` listener too, so a connectivity change that happened while the WKWebView was suspended in the background still gets caught the moment the app is foregrounded again, not just on a genuine `online` event firing live.
- [x] **Watch-side offline behavior — scoped to graceful degradation, not a Watch-local queue** (explicit choice, discussed with the user: a real offline queue on the Watch itself would be a sizeable standalone feature, not a Phase 5 polish item). Audited `WatchAPIProxy.swift` (the no-phone-app-open native fallback) and `PhoneWorkoutObserver.swift` (the Watch's own request path) — turned out to already be in good shape: every network/server failure surfaces a clear, German, on-screen message (`SetEntryView`/`KraftLoggingView` both show it inline, keep the entered weight/reps, and let the user just retry), not a silent drop. The one real gap: `sendMessage`'s raw transport `errorHandler` passed an untranslated English `NSError` straight through, inconsistent with every other failure path in the app. Fixed by normalizing it into the same German `RequestError` family (`"Verbindung zum iPhone unterbrochen — bitte erneut versuchen"`).
- [x] **Verified via `xcodebuild`** (full `App` scheme, iOS Simulator SDK): compiles clean aside from the pre-existing, unrelated `FitTrackComplicationExtension` `#Preview` iOS-17 issue already documented in the Phase 2 log — confirmed via grep that no errors trace back to `PhoneWorkoutObserver.swift` or the Watch target. `npx tsc --noEmit` clean for the JS change.
- [ ] **On-device test needed**: airplane mode toggled mid-session on the phone while a Watch-driven workout is active — confirm the Watch shows the new clear error message instead of a raw/English one, and that re-enabling connectivity lets a retry succeed. Separately, confirm the phone-side queue flush now also fires reliably right after backgrounding-then-foregrounding the app with connectivity restored in between (not just a live `online` event).

## Explicitly out of scope for now
- Real conflict resolution beyond last-write-wins (revisit only if it causes actual problems in testing)
- Offline support for AI/coach features (inherently need a live model call)
- Multi-device simultaneous-edit conflicts

## Log
- 2026-07-10: Investigated current architecture (`capacitor.config.ts`, 17 server-auth pages, no existing local-storage plugin). Wrote this roadmap. Starting Phase 1.
- 2026-07-10: Phase 1 code complete — native login route, Keychain token plugin, `authenticatedFetch` helper, wired additively into the login form. Verified server-side end-to-end; on-device Keychain test still pending (needs Xcode/real device).
- 2026-07-10: Discovered a full Xcode/xcodebuild toolchain is available in this environment — used it to compile the whole App target (App + Watch + Complication + RestTimerWidget) and to standalone-verify the Keychain save/load/clear logic against the real Security framework before asking for on-device confirmation. Root-caused an early "always undefined" false alarm to an accidental `git checkout main` (from an earlier interrupted merge attempt) silently leaving the working tree on the wrong branch, so Xcode was rebuilding a version without the new plugin file at all — fixed by switching back to `feature/offline-first`. Phase 1 fully confirmed on-device afterward: save/load/clear round trip correct in the real Keychain via Xcode console + Safari Web Inspector. Phase 1 merged to `main`.
- 2026-07-10: Started Phase 2 on `feature/offline-phase2-local-bundle`. Converted the `(app)` layout and all 18 server-auth-gated pages to client components; converted 13 API routes to dual cookie/Bearer auth; added the global fetch patch; wired up `output: "export"` for native builds via `scripts/build-native.mjs` (moving `middleware.ts`/`src/app/api`/`src/app/.well-known`/`src/app/manifest.ts` outside `src/app/` for the duration of that build only — renaming *within* `src/app/` doesn't work, Next.js still treats it as a route). Iteratively fixed each newly-surfaced static-export blocker (missing `generateStaticParams()` on 4 dynamic routes via thin server-wrapper + client-component split; root layout's `resolveAppLocale()` calling `cookies()`/`auth()`/Prisma at build time). `npm run build:native` now succeeds end-to-end (38 pages, 0 errors), confirmed the normal Vercel build is unaffected, wired `capacitor.config.ts` to the exported bundle (dropped `server.url`), and verified via `xcodebuild` that the full App target compiles against the synced output.
- 2026-07-10: First real-device test caught a bug the build couldn't: login failed silently, endlessly redirecting back to `/login` (Xcode console showed `NativeAuthToken load` → `{"token":null}` forever — the native-login POST was never actually landing). Root cause: with the UI now served from `capacitor://localhost` instead of the live Vercel origin, every `fetch("/api/...")` became a genuine cross-origin request, and none of the API routes send CORS headers (they never needed to before this phase) — WKWebView silently blocks these exactly like a browser would.
- 2026-07-10: Extended on-device debugging to get Phase 2 actually working. The API layer (dual-auth routes + 3 bundling routes) was split into `feature/offline-phase2-api-layer` and merged to `main` first, since it's backward-compatible and the native app calls production — verified live (`/api/records` etc. return JSON with a Bearer token). Then, on the native branch, a chain of native-only issues was root-caused and fixed:
  - **CORS / cross-origin API calls:** rather than the blanket `plugins.CapacitorHttp.enabled` flag (which also rewrote Next's internal RSC fetches and forced a full reload on every tab tap), the fetch patch now calls `CapacitorHttp.request()` **only** for `/api/*` URLs, leaving Next's own same-origin navigation fetches untouched.
  - **Login MissingCSRF:** WKWebView's cookie jar and the native HTTP bridge's cookie store don't stay in sync, so NextAuth's CSRF cookie never survived the csrf→callback round trip. On native the Bearer token (`/api/auth/native-login`) is now the authoritative credential; the cookie `signIn` runs alongside best-effort and no longer gates login. Cookie sync via `CapacitorCookies` is scoped to `/api/auth/*` only.
  - **WebView unresponsive / taps ignored:** every request was doing a Keychain read over the bridge (plus one per navigation in `RequireAuth`), saturating the bridge. Added an in-memory token cache (`auth-token-cache.ts`) — read once, reused, self-heals on 401; `useAuthGate` resolves synchronously from it after the first read.
  - **THE navigation bug ("nothing opens"):** an on-device click/route probe proved taps registered but the route never changed for prefetched Links (`/workouts`, `/health`), while non-prefetched ones (`/more`, `router.push`) worked. Next's App Router prefetch fetches an RSC variant that doesn't resolve over `capacitor://localhost` and poisons the router cache so the tap no-ops. Fixed with an `app-link.tsx` wrapper defaulting `prefetch={false}`, swapped in for `next/link` across all 33 files, plus removal of every explicit `prefetch` prop. Prefetch buys nothing in a fully-local bundle anyway.
  - **"No health data":** four health pages fetched client-side and passed a defined-but-empty (`[]`/`null`) initial prop to detail components that self-fetch; those components read "prop present" as "data already provided" and skipped their own fetch. Fixed by letting the components self-fetch (no initial props), matching the ExercisesPage/metric pattern.
- 2026-07-11: **Phase 3 verified on-device (iPad) and merged to `main`.** Airplane mode + force-quit + relaunch showed cached data across screens as designed. Two auth bugs surfaced and fixed during this test: a misleading "invalid email or password" message when actually offline (a thrown fetch is indistinguishable from a rejected credential without checking `navigator.onLine`), and an authenticated user stranded on the login/landing screen offline because `RedirectIfAuthenticated` — the client-side equivalent of `middleware.ts`'s authed-user redirect, written in Phase 2 — was never actually wired into any page (`git log -S` found zero uses in `src/app`). Removed the temporary `NativeDebugOverlay` diagnostic before merging.
- 2026-07-11: Started Phase 4. Before writing any new code, audited the existing offline write-queue infrastructure and found it already substantially complete from the pre-roadmap PWA effort (queue tables, op-replay-with-ID-remapping flush logic, app-wide auto-flush-on-reconnect) and — checked specifically for the Phase 3 "written but never wired in" failure mode — actually used throughout `workout-detail.tsx`/`set-row.tsx`/`workouts/new/page.tsx`. Its fetch calls use the same relative-path + `credentials: "include"` pattern the global `NativeAuthFetchPatch` already intercepts app-wide, so it should work under the native Bearer-token architecture without changes — not yet confirmed on-device, which is the one remaining gap before this phase can be marked done.
- 2026-07-11: On-device Phase 4 testing blocked by two newly-discovered bugs, both fixed:
  - **Opening any workout triggered a full WebView reload and re-locked Face ID.** Root cause: navigating client-side to `/workouts/<realId>` needs an RSC payload Next can't resolve under `output: "export"` (only a placeholder param is pre-rendered, per Phase 2), so it falls back to a hard reload. Fixed at the source: added `workoutHref()` (`src/lib/workout-href.ts`) which routes native navigation through the pre-rendered placeholder path (`/workouts/_?id=<realId>`) instead, read back out via `useSearchParams()` in `page-client.tsx` — no more reload at all. Web keeps the clean path-based URL since its server can resolve any id. As a second line of defense, `NativeAppLock` now persists the last successful unlock timestamp in `localStorage` (survives a full reload, unlike the previous in-memory ref) and skips re-locking on mount if within `REAUTH_GRACE_MS`.
  - **Face ID canceled outright on offline cold launch** (`BiometricLock.authenticate()` rejecting with "Authentication canceled"). Root cause: `NativePushRegister`, `NativeHealthSync`, `NativeWatchWorkoutSync`, and `OfflineSyncProvider` were mounted as siblings of `NativeAppLock` in `providers.tsx`, so they all fired immediately and concurrently with the Face ID prompt — push registration in particular can trigger the system "Allow Notifications?" alert, and a competing system dialog reliably cancels a concurrent LocalAuthentication session. Fixed by nesting all four inside `<NativeAppLock>` so they only start after a successful unlock.
- 2026-07-11: Offline "start a blank workout" then confirmed working (add exercise, log sets, delete a set), but **finishing/canceling a workout appeared completely unresponsive** — tapping either button did nothing, no error. Two separate bugs stacked here:
  - `window.confirm()` (used to gate both actions) was silently failing to show its native panel in WKWebView — most likely the same class of issue as the Face ID cancellation above (a prior system alert/prompt leaves the WKWebView JS-dialog bridge in a bad state). Replaced with an in-app confirm dialog (`askConfirm()` in `workout-detail.tsx`, using the existing `Dialog` primitive) for all four `confirm()` call sites in that file — sidesteps the native dialog bridge entirely, verified working both in-browser and on-device.
  - Once the dialog itself was fixed, cancel worked but finish still silently did nothing: `completeWorkout()`, unlike `cancelWorkout()`, had **no try/catch anywhere** — any failure (a rejected fetch online, an IndexedDB error offline) left the button stuck on "Finishing…" forever with no visible error. Hardened both branches with try/catch + a user-facing failure message (`workouts.finishFailed`), matching `cancelWorkout()`'s existing defensiveness.
  - Even after both of those fixes, finish alone (not cancel) still didn't respond on-device. Root cause: the floating rest-timer bar (`rest-timer-bar.tsx`) is `fixed inset-x-0 top-0` — full viewport width for centering — but had no `pointer-events-none`, so its empty edges (everywhere the visible capsule isn't) silently absorbed taps on whatever sat underneath. On a narrow phone screen the stacked "Finish workout" button landed in that dead zone right under the header; "Cancel workout", lower down, escaped it — explaining the asymmetric symptom. Fixed by making the outer wrapper `pointer-events-none` and only the inner capsule `pointer-events-auto`.
  - **Confirmed fully working on-device afterward**: offline start, log sets, add exercise, delete a set, cancel, and finish all verified end-to-end. Phase 4 complete.
- 2026-07-10: **Phase 2 verified end-to-end on-device (iPad).** Online: login, navigation, and every screen load real data. Offline (airplane-mode relaunch): the app opens to the full shell with no white screen; data is absent as designed (Phase 3). Acceptance criterion met — ready to merge to `main`. (Leftover cleanup for later: `src/components/native-debug-probe.tsx` and `src/features/auth/actions/register.ts` are now dead code but the sandbox blocks `rm` — delete manually.)
- 2026-07-11: Started Phase 5 on `feature/offline-phase5-background-sync`. Investigating "auto-flush via BackgroundSyncManager" surfaced an architectural dead end (see checklist above) — `BGAppRefreshTask` has no WKWebView, so it can't touch the JS-side IndexedDB queue at all; scoped down to a `resume`-triggered flush check instead. Discussed Watch-offline scope with the user directly: full local queue on the Watch vs. graceful-degradation-only — chose graceful degradation, since a real Watch-side queue is its own sizeable feature, not a polish pass. Audit found the Watch's error handling already mostly solid; fixed the one inconsistent raw-error path in `PhoneWorkoutObserver.swift`. Verified via `xcodebuild` (clean aside from the pre-existing, documented, unrelated Complication `#Preview` issue) and `tsc --noEmit`. On-device test still needed before merge.
